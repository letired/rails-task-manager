<h1>To Do:</h1>

<ul>
  <% @tasks.each do |task| %>
   <li>
      <h2><%= link_to task.name, "/tasks/#{task.id}" %></h2>
      <p><%= task.details %></p>
      <% if task.complete  %>
        <h3>This task is done!</h3>
      <% end %>
    </li>
  <% end %>
</ul>
